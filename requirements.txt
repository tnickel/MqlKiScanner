# Kern (Phase 0-2, laeuft ohne LLM-Key)
requests>=2.31
beautifulsoup4>=4.12
pandas>=2.0
# PDF-Berichte (Unicode-Schriften sind im Paket enthalten; keine Systemfonts nötig)
reportlab>=4.2,<5
# GUI (Phase 4) inklusive eingebettetem PDF-Viewer
streamlit[pdf]>=1.63,<2
# MQL5-Login/Export-Fallback (Selenium-Chrome, persistentes Profil)
selenium>=4.15
# LLM-Layer (Phase 3): GLM ueber die OpenAI-kompatible Z.ai-REST-API,
# kein zusaetzliches SDK noetig (requests genuegt).
